import { defineConfig } from 'jsrepo'

export default defineConfig({
  registries: ['https://raw.githubusercontent.com/DavidHDev/react-bits/main/public/r'],
  paths: {
    component: 'components/bits',
  },
})
